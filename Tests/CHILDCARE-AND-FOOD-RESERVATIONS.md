# Childcare continuation and food reservation checks

Regression source: the fR650Vv7Et session, audited on 2026-09-05. Gonzalez's successful Breastfeed completed the Work buffer; AOM then replaced the native BringBabyToSafety finalizer with locker return. Denton was carried to the locker and dropped during gear restoration. Native FeedBaby.MakeNewToils supplies the safety finalizer, which covers both breast and bottle feeding.

The fix admits that existing native safe-placement job only for an exact carried baby with an AOM outfit state or Non-Work retention allowance. Recall and idle outfit recovery wait for placement; successful feeding still counts, while its placement follow-up does not earn a second buffer credit. Job identity is transient and keyed by caregiver, baby, map and load ID; no duplicate Job is serialized. Area boundary, permission and hazard checks remain in force. Drafted, downed and mental-state control supersede the deferral. Native childcare chooses the crib or other safe destination and remains responsible for interrupted/failed placement; AOM does not create a replacement childcare job or force entry into a restricted area.

Run `run-meal-handoff-contracts.ps1`: 89 checks, including 24 new checks against the production ChildcareContinuation and NonWorkBufferTracker. They cover full buffers, successful feeding, placement finalization after release, identity reuse, map/load boundaries, failure and native overrides. These tests use a deterministic native API model, not a running game.

Run `run-ingest-reservation-contracts.ps1`: 17 checks against the production admission helper. Native Ingest reserves with maxPawns 10 and FoodUtility.GetMaxAmountToPickup, rather than raw Job.count. The helper checks that same amount before accepting automatic map-food ingestion. It neither acquires nor clears another pawn's reservation. Explicit orders, held meals, paste dispensers and custom drivers keep their existing handling. A job-giver rejection permits the think tree to continue; a late StartJob rejection yields briefly before native reselection.

The repeated empty-path warnings remain unassigned to a root cause. Detailed PatherFailed logging captures the pawn, exact job/ID/targets, start/destination, active restricted rules, activity permissions and missing outfit context. Messages are coalesced for 600 ticks. This does not alter routing or grant access. The native method, pawn field and destination property were verified against the installed Assembly-CSharp.dll.

Manual follow-up: finish the buffer while feeding a carried baby; confirm safe placement precedes locker return and personal restoration. Repeat with bottle feeding, a missing/occupied crib, interruption, another caregiver, and save/load. Check the former contested food stack and capture any new path-failure context. Compilation and controller tests do not establish a native gameplay pass.
